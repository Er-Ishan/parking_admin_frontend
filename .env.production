# Same-origin proxy so session cookie works on Safari/iOS (avoids cross-origin cookie blocking).
# Frontend calls /api/backend/*; server forwards to API_BACKEND_URL with the cookie.
# Changed by Qasim - 2025-02-20
NEXT_PUBLIC_API_BASE_URL=/api/backend
API_BACKEND_URL=https://help.bookheathrowparking.co.uk
NODE_ENV=production